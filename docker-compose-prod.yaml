services:
  nextjs:
    container_name: nextjs-prod
    build:
      context: ./
      dockerfile: Dockerfile.prod
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production